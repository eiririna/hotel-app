<!-- Main modal -->

<div data-controller = "modal" data-action = "click->modal#showModal" id="authentication-modal"  tabindex="-1" aria-hidden="true" class="hidden container mx-auto overflow-y-auto overflow-x-hidden fixed top-0 right-0 left-0 z-50 w-full md:inset-0 h-modal md:h-full">
  <div class="relative container content-center mx-auto relative w-full max-w-md h-full md:h-auto">
    <!-- Modal content -->
    <div class = "hidden" id="modal-trigger" data-action="click->modal#showModal"></div>
    <div id = "modal-panel" class="relative container content-center bg-white rounded-lg shadow dark:bg-gray-700 content-center" >
    <%= render "home/user_auth_form" %>
    </div>
  </div>
</div>
